---
import MenuBar from '../Svgs/menu-bar.svg';
---

<nav id="site-navigation" class="main-navigation">
    <a href="#top-back"><button class="menu-toggle" aria-controls="primary-menu" aria-expanded="false">
        
        <object data={MenuBar.src} type="image/svg+xml" width="50" height="50"></object>
    </button></a>
	
    <ul class="primary-menu">
        <li class="menu-item"><a href="#top-back"> about me</a></li>
        <li class="menu-item"><a href="#articles"> my work</a></li>
        <li class="menu-item"><a href="#my-skills"> my skills</a></li>
        <li class="menu-item"><a href="#contact-me"> contact me</a></li>

    </ul>
</nav><!-- #site-navigation -->
<div class="nav-container">
    <a href="/">
        <!-- <h1 class="site-icon">(^_<span class="rotate eye">*</span> );</h1> -->

        <svg version="1.1" id="salwa-coding" xmlns="http://www.w3.org/2000/svg"
            xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" width="100" height="80"
            viewBox="0 0 450 450" xml:space="preserve">
            <g class="face">
                <path id="start-curve" fill="#fff" stroke-width="2" d="M59.62,335.79c-31.64-20.85-45.78-54.82-45.78-93.46v-0.15c0-38.49,14.14-72.02,45.78-93.31l4.37,5.1
     c-21.72,18.08-33.82,51.9-33.82,88.35c0,36.6,12.54,70.71,33.82,88.65L59.62,335.79z" />
                <path id="smile-eye" fill="#fff" stroke-width="2"
                    d="M166.04,255.17l-32.95-73.04L100,255.17H87.46l40.97-87.48h10.06l40.97,87.48H166.04z" />
                <path id="mouth" fill="#fff" stroke-width="2"
                    d="M193.3,326.46v-10.64h106.14v10.64H193.3z" />
                <path id="star-eye" fill="#FF8C28" transform-origin="150px 70px" d="M349.74,200.78l8.31,29.45l-5.1,2.19l-15.31-27.85l-26.24,16.33l-3.21-4.81l22.02-20.27l-22.45-22.02l3.21-4.52l26.24,17.2
l12.54-28.43l5.39,1.31l-5.83,29.89l28.87,0.58v5.54L349.74,200.78z" />
                <path id="end-curve" fill="#fff" stroke-width="2" d="M446.4,242.63c0,38.34-13.85,71.73-46.07,93.16l-4.23-4.81c21.87-18.37,33.97-52.05,33.97-88.65
c0-36.45-12.98-71.15-33.97-88.5l4.23-4.96c32.08,21.43,46.07,54.82,46.07,93.46V242.63z" />
                <path id="smai-colon" fill="#fff" stroke-width="2" d="M482.7,271.21c8.02,0,20.56,8.75,20.56,23.76c0,17.06-15.75,26.68-29.74,30.62l-2.04-6.56
c10.79-4.37,16.91-10.21,16.91-18.52c0-7.44-6.12-12.54-14-14.43c-1.02,0-2.92-1.9-2.92-5.69c0-5.54,5.39-9.18,10.93-9.18H482.7z
M487.07,212.45c7.29,0,12.54,5.25,12.54,12.98c0,7.58-5.25,12.98-12.68,12.98s-12.83-5.39-12.83-12.98
c0-7.73,5.39-12.98,12.83-12.98H487.07z" />
            </g>
        </svg>
    </a>
	<slot/>
    <div id="scroll-progress"></div>
</div>
<style>
    /* @-moz-keyframes marquee {
	0% {
	  transform: translateX(100%);
	}
	100% {
	  transform: translateX(-100%);
	}
  }
  @-webkit-keyframes marquee {
	0% {
	  transform: translateX(100%);
	}
	100% {
	  transform: translateX(-100%);
	}
  }
  @keyframes marquee {
	0% {
	
	  left: -20%;
	}
	100% {
	  -moz-transform: translateX(-100%);
	  -webkit-transform: translateX(-100%);
	  transform: translateX(-100%);
	  left: 120%;
	}
  } */
/* scroll progress bar------ */
/* #scroll-progress {
	position: absolute;
	bottom: 0;
	width: 0%;
	height: 4px;
	background: #FF8C28;
	z-index: 10000;
} */

/* menu styling----------------- */
/*.main-navigation ul.primary-menu {
	 padding-top: 5rem;
	position: absolute;

	background-color: transparent;
	top: 0;
	left: 0;
	width: 90%;
	height: 100vh;
	z-index: 0;
}

.main-navigation.toggled ul.primary-menu li {
	display: block;
	margin: .5rem;
	width: 50%;
	border-bottom: 1px solid rgba(7, 29, 54, 0.8);
		transition: border-width .1s ease-in-out;
	
	width:100%;
}

.main-navigation.toggled ul.primary-menu li a {
	
	padding: 1rem;
	text-transform: capitalize;
	border-left: 2px solid rgba(7, 29, 54, 0.8);
	
	font-weight: bold;
	color: hsl(212, 77%, 12%);

}

.main-navigation.toggled ul.primary-menu li:hover {
	border-left-width: 3px;
	border-bottom-width: 2px;
	font-weight: bolder;
}

.menu-toggle {
	background-color: transparent;
	outline: none;
	border: none;

	color: #FF8C28;
	position: fixed;
	top: 0;
	right: 0;

	z-index: 2000;
}

button.menu-toggle .dashicons-menu-alt,
button.menu-toggle .dashicons-menu-alt:before {
	transition: transform .3s ease-in-out;
	will-change: content;
	font-size: 3rem;
	width: 100%;
	height: 100%;
}

.menu-toggle.visible-menu {
	right: unset;
	left: 0;
}


.scroll-disapple {
	overflow-x: hidden;
	overflow-y: hidden;
	
	

} */
/* set proper hight to the contat to get equal rotate in all pages it doesnt need to be spisfic value as the overflow well be hidden */
/* .scroll-disapple .page-rotate{
	
	height: 176rem;
}
.page-rotate
 {


	transform: translateX(-30vw) rotate(10deg);
}
.single-work.page-rotate{
	transform: translateX(22vw) translateY(7%) rotate(10deg);
}
.main-navigation.toggled button.menu-toggle .dashicons-menu-alt,
.main-navigation.toggled button.menu-toggle .dashicons-menu-alt:before {
	content: "\f335";
	font-family: 'Dashicons';

}
.main-navigation button.menu-toggle .dashicons-menu-alt,
.main-navigation button.menu-toggle .dashicons-menu-alt:before {
	content: "\f228";
	font-family: 'Dashicons';

}
@media screen and (min-width: 34.5em) {

	.page-rotate{
	
		transform: translateX(5%) translateY(1.75%) rotate(5deg);
	
	}
	.single-work.page-rotate{
		transform: translateX(22vw) translateY(7%) rotate(10deg);
	}
	
}

@media screen and (max-width:47.5rem) {
	.page-rotate{
	
		transform: translateX(2rem) translateY(1vw) rotate(5deg);
	
	}
	.single-work.page-rotate{
		transform: translateX(22vw) translateY(7%) rotate(10deg);
	}
	.header-info {
		flex-wrap: wrap;
	}
	.header-info .site-branding{
		margin-right: 0;
		flex-basis: 100%;
		text-align: center;
	}
	.header-info .site-branding .dev-name{
		display: table;
		margin: auto;
	}
} */

</style>

<script>
	
     gsap.to("#scroll-progress",{
       width: "100%",
      scrollTrigger:{
      scrub: 0.3,//lock the animation to scrollbar position
         toggleActions:'restart reverse none none',      
         
      }
  })
    //  // navigation-------------------------

( 
	
	function() {
		
	
	const siteNavigation = document.getElementById( 'site-navigation' );
	const htmlPage = document.querySelector('body');
	const page = document.querySelector('.site');
	const toggleBtn = document.querySelector('.menu-toggle');
	const mainMenu = document.querySelector('.main-navigation');
	const   mainMenuItmes = document.querySelectorAll('.main-navigation .menu-item a');
	// Return early if the navigation doesn't exist.
	if ( ! siteNavigation ) {
		return;
	}

	const button = siteNavigation.getElementsByTagName( 'button' )[ 0 ];

	// Return early if the button doesn't exist.
	if ( 'undefined' === typeof button ) {
		return;
	}

	const menu = siteNavigation.getElementsByTagName( 'ul' )[ 0 ];

	// Hide menu toggle button if menu is empty and return early.
	if ( 'undefined' === typeof menu ) {
		button.style.display = 'none';
		return;
	}

	if ( ! menu.classList.contains( 'nav-menu' ) ) {
		menu.classList.add( 'nav-menu' );
	}

	// Toggle the .toggled class and the aria-expanded value each time the button is clicked.
	button.addEventListener( 'click', function() {
		siteNavigation.classList.toggle( 'toggled' );

		if ( button.getAttribute( 'aria-expanded' ) === 'true' ) {
			button.setAttribute( 'aria-expanded', 'false' );
		} else {
			button.setAttribute( 'aria-expanded', 'true' );
		}
	} );
	
	// Remove the .toggled class and set aria-expanded to false when the user clicks outside the navigation.
	document.addEventListener( 'click', function( event ) {
		const isClickInside = siteNavigation.contains( event.target );

		if ( ! isClickInside ) {
			
			page.classList.remove('page-rotate');
			htmlPage.classList.remove('scroll-disapple');
			toggleBtn.classList.remove('visible-menu');
			siteNavigation.classList.remove( 'toggled' );
			button.setAttribute( 'aria-expanded', 'false' );
		}
	} );


	  function menuRotate() {
		  if (mainMenu.classList.contains('toggled')) {
			  page.classList.add('page-rotate');
			  htmlPage.classList.add('scroll-disapple');
			  toggleBtn.classList.add('visible-menu');
		  }else{
			  page.classList.remove('page-rotate');
			  htmlPage.classList.remove('scroll-disapple');
			  toggleBtn.classList.remove('visible-menu');
		  }
	  }
	  toggleBtn.addEventListener('click', menuRotate);
	  mainMenuItmes.forEach((menuItem)=>{
		menuItem.addEventListener("click",()=>{
			page.classList.remove('page-rotate');
			htmlPage.classList.remove('scroll-disapple');
			toggleBtn.classList.remove('visible-menu');
			siteNavigation.classList.remove( 'toggled' );
			button.setAttribute( 'aria-expanded', 'false' );
		})
	  })
	  //   page.addEventListener('click',menuRotate);
	// /////////////////////////////////
}() );

    
</script>